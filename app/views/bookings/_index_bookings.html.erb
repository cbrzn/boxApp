<class="bookings">
<% if current_user.bookings(@training_id) %>
<%= link_to "Usuarios reservados", box_training_bookings_path(@training.box, @training), class: "btn btn-default"  %>

<% end %>
</class>
