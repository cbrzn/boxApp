<li id="booking-<%= booking.id %>">
  <span class="user"><%= link_to booking.user.name, booking.user %></span>
  <span class="timestamp">
  reservado hace <%= time_ago_in_words(micropost.created_at) %>
</span>
</li>
