Reinicio de contrasena

Para reiniciar su contrasena haga click en el siguiente enlace:

<%= link_to "Reiniciar contrasena", edit_password_reset_url(@user.reset_token,
                                                      email: @user.email) %>

El enlace expirara en 2 horas.

Si usted no pidio que su contrasena fuese reiniciada, por favor ignore este mensaje
y su contrasena seguira siendo la misma.
